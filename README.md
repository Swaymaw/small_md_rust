## Description:

A tiny markdown compiler based on Jesse's Tutorials. Made using Rust.

## Usage:

```zsh
    tinymd <input_file.md>
```

## TO DO LIST:

- [x] Take MD file as an input and compile it to HTML
- [x] Add support for first heading tag h1 and paragraph tag p.
- [ ] Make the code more modular
- [ ] Improve the user input logic and allow more robust inputs.
- [ ] Allow folder path as input and convert all md files inside.
- [ ] Allow optional output path.
- [ ] Add support for all heading tags (h2, h3, h4, h5, h6).
- [ ] Add support for a tag.
- [ ] Add support for em and strong tags.
- [ ] Add support for image tag.
- [ ] Add support for br and hr tags.
